# ===== .NET =====
bin/
obj/
out/

# Build results & packages
*.user
*.suo
[Dd]ebug/
[Rr]elease/
artifacts/
*.nupkg

# EF Core / runtime junk
*.mdf
*.ldf
*.ndf

# ===== IDEs =====
# Visual Studio
.vs/
# Rider
.idea/
# VS Code (keep shared settings if you want — then remove this line and ignore selectively)
.vscode/

# ===== Secrets & local config =====
# NEVER commit these — connection strings, API keys, JWT secrets live here
.env
.env.local
appsettings.Development.json
appsettings.Local.json
*.pfx

# ===== Frontend (React/Vite — for when EPIC 7 lands in this repo) =====
node_modules/
dist/
dist-ssr/
*.local
.eslintcache

# Frontend env files (Vite exposes VITE_* vars — still never commit real ones)
client/.env
client/.env.*
!client/.env.example

# ===== Test & coverage output =====
TestResults/
coverage/
*.trx
coverage.*.xml

# ===== OS noise =====
.DS_Store
Thumbs.db